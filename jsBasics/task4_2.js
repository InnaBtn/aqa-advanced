let rectangleLength = 12;
let rectangleWidth = 6;
let rectangleArea = rectangleLength * rectangleWidth; // Calculate the area of a rectangle using the formula length * width.

console.log(rectangleArea);